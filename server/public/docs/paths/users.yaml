get:
  summary: List users
  tags:
    - users
  responses:
    '200':
      description: An array of users
      content:
        application/json:
          schema:
            $ref: '../schemas/users.yaml'